server:
  port: 8080
spring:
  application:
    name: user-service # 服务名称
  cloud:
    nacos:
      config:
        group: dev
        namespace: e2a06f55-6efb-4898-a990-f43c9f455579
        server-addr: 1.12.42.148:8848 # Nacos地址
        file-extension: yaml # 文件后缀名
        shared-configs:
          - dataId: douyin-common.yaml
            group: dev
            refresh: true
        extension-configs:
          - dataId: douyin-redis.yaml
            group: dev
            refresh:  true
          - dataId: douyin-dubbo.yaml
            group: dev
            refresh: true
dubbo:
  cloud:
    subscribed-services: user-service