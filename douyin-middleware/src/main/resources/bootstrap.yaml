server:
  port: 8080
spring:
  # 服务名称
  application:
    name: douyin-middleware
  cloud:
    nacos:
      config:
        group: dev
        namespace: e2a06f55-6efb-4898-a990-f43c9f455579
        server-addr: 1.12.42.148:8848
        file-extension: yaml
        shared-configs:
          - dataId: douyin-common.yaml
            group: dev
            refresh: true
        extension-configs:
          - dataId: douyin-minio.yaml
            group: dev
            refresh:  true
          - dataId: douyin-dubbo.yaml
            group: dev
            refresh: true
# 消费服务
dubbo:
  cloud:
    subscribed-services: user-service